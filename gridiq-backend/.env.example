ENV=dev
# Hostname `db` only works inside Docker Compose. For uvicorn/alembic on your PC, use localhost
# and run Postgres (e.g. `docker compose up -d db`).
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/gridiq

JWT_SECRET=change-me
JWT_ALG=HS256
ACCESS_TOKEN_MINUTES=30
